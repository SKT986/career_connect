import { ComingSoon } from "@/components/shared/coming-soon";
import { Building2 } from "lucide-react";

export default function CompaniesPage() {
  return (
    <ComingSoon
      icon={Building2}
      title="Company Matching"
      description="Soon you'll be able to build an anonymous profile — skills, goals, languages, accommodations — and choose exactly when and with whom to reveal your identity to inclusive employers."
    />
  );
}
